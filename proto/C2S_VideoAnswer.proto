syntax = "proto3";
package video_answer;

option java_outer_classname="C2SVideoAnswer";

// cmd - 141 接听
// cmd - 142 拒接
// cmd - 143 取消
// cmd - 144 挂断
message VideoAnswer
{
	string roomId = 1;	// 音视频通话房间ID
	string userId = 2;  // 操作对象用户ID  取消/挂断的对象id
}
